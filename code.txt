int x = 5;
float y = 3.14;
bool isActive = false;
string name = "Gabriel";
int sum = x + 10;
int product = x * 2;
float result = (y / 2) + x - 1.5;

tree arvore = [];
queue fila = [];
stack pilha = [];

if (x < y) {
    int z = sum * product;
    bool flag = (x < y);
}

while (x < 2) {
    int c = 0;
    c++;
}
